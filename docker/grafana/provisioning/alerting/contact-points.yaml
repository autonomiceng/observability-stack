apiVersion: 1
contactPoints:
  - orgId: 1
    name: configure-delivery
    receivers:
      - uid: configure-delivery
        type: email
        disableResolveMessage: false
        settings:
          addresses: configure@example.invalid
policies:
  - orgId: 1
    receiver: configure-delivery
    group_by: [grafana_folder, alertname]
# Bootstrap replaces this template in OB_STATE_DIR/grafana-provisioning.
# Without webhook or email delivery settings this placeholder stays and readiness reports degraded.
